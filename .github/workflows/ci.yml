name: ci
env:
  ACTIONS_ALLOW_UNSECURE_COMMANDS: true
on: 
  schedule:
    - cron: '0 0 * * *'
  push:
  workflow_dispatch:

jobs:
  # record:
  #   runs-on: ubuntu-20.04
  #   outputs:
  #    recordDuration: $(( $UPLOAD_START - $RECORD_START ))
  #    uploadDuration: $(( `date +%s` - $UPLOAD_START ))
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v3
  #     # Install NPM dependencies, cache them correctly
  #     # and run all Cypress tests
  #     # https://github.com/cypress-io/github-action
  #     - name: Set test start
  #       run: echo "::set-env name=RECORD_START::date +%s\n"
  #     - name: Cypress run
  #       uses: cypress-io/github-action@v4
  #       with:
  #         browser: 'Replay Chromium'
  #         # check the spec types
  #         build: npm run lint
  #         # start the application before running Cypress
  #         start: npm start
  #       env:
  #         RECORD_REPLAY_TEST_METRICS: 1
  #         RECORD_REPLAY_WEBHOOK_URL: ${{ secrets.RECORD_REPLAY_WEBHOOK_URL }}
  #         RECORD_ALL_CONTENT: 1
  #         RECORD_REPLAY_METADATA_FILE: /tmp/replay-metadata
  #         RECORD_REPLAY_METADATA_TEST_RUN_TITLE: "help i'm trapped in a test run title making factory"
  #     - name: Set upload start
  #       run: echo "::set-env name=UPLOAD_START::date +%s\n"
  #     - name: Upload replays ▶️
  #       if: ${{ always() }}
  #       uses: replayio/action-upload@v0.4.3
  #       with:
  #         api-key: rwk_3gdLWqm8nrWA7jbPG8aiADVQ7moc0PYGdAEdnKAfzbM
  #         public: true
  # no-record:
  #   runs-on: ubuntu-20.04
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v3
  #     # Install NPM dependencies, cache them correctly
  #     # and run all Cypress tests
  #     # https://github.com/cypress-io/github-action
  #     - name: Cypress run
  #       uses: cypress-io/github-action@v4
  #       with:
  #         browser: 'Replay Chromium'
  #         # check the spec types
  #         build: npm run lint
  #         # start the application before running Cypress
  #         start: npm start
  #       env:
  #         RECORD_REPLAY_TEST_METRICS: 1
  #         RECORD_REPLAY_WEBHOOK_URL: ${{ secrets.RECORD_REPLAY_WEBHOOK_URL }}
  #         RECORD_ALL_CONTENT: 1
  #         RECORD_REPLAY_METADATA_FILE: /tmp/replay-metadata
  #         RECORD_REPLAY_DRIVER: /tmp/not-here
  #         RECORD_REPLAY_METADATA_TEST_RUN_TITLE: "help i'm trapped in a test run title making factory"
  original:
    runs-on: ubuntu-20.04
    outputs:
     duration: $(( ${{steps.end.outputs.time}} - ${{steps.start.outputs.time}} ))
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      # Install NPM dependencies, cache them correctly
      # and run all Cypress tests
      # https://github.com/cypress-io/github-action
      - name: Set test start
        id: start
        run: |
          echo "time=`date +%s`" >> $GITHUB_OUTPUT
      - name: Cypress run
        uses: cypress-io/github-action@v4
        with:
          # check the spec types
          build: npm run lint
          # start the application before running Cypress
          start: npm start
      - name: Set test end
        id: end
        run: |
          echo "time=`date +%s`" >> $GITHUB_OUTPUT
  upload-benchmarks:
    name: Upload benchmarks
    runs-on: ubuntu-latest
    needs: [original]
    # needs: [record, original]
    steps:
      - run: |
          echo "Test Duration: ${{ needs.original.outputs.duration }}"
      #     echo "Record Duration: ${{ needs.record.outputs.recordDuration }}"
      #     echo "Upload Duration: ${{ needs.record.outputs.uploadDuration }}"
      # - run: |
      #     echo $TEST_START
      #     echo "Test Duration: $(( `date +%s` ))"
      #     echo "Test Duration: $(( `date +%s` - 10 ))"
      # - run: |
      #     echo "Duration ${{ needs.original.outputs.duration }}"


